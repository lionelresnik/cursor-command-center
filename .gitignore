# User-specific configuration (generated by setup.sh)
config.json
.last-workspace
.last-browse-dir

# ============================================
# LOCAL KNOWLEDGE BASE (never pushed)
# Use ./cc export to backup, ./cc import to restore
# ============================================

# Task history - work logs (user-specific)
task-history/**/*.md
!task-history/README.md

# Reference docs - guides, how-tos (user-specific, may contain credentials)
docs/
!docs/README.md

# Todos and standups (user-specific)
todos.md
standups/

# Generated workspace files (each user generates their own)
workspaces/*.code-workspace
*.code-workspace

# Demo/recording files
*.tape

# Context selections (user-specific)
contexts/*.selection
contexts/*.dirs
contexts/*.repos

# macOS
.DS_Store

# IDE
.idea/
*.swp
*.swo

# Keep the directories but ignore generated content
!workspaces/.gitkeep
!contexts/.gitkeep
!task-history/.gitkeep
!docs/.gitkeep

.cursor/cc-context.json
.cursor/cc-last-pr.txt
